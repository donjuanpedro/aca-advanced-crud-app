<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Grocery List</title>
    </head>
<body>
  <form action="/items" method="POST">
      <input type="text" name="post">
      <input type="submit" value="+ Add Post">
  </form>

  <ul>
      <% for (idx in posts) { %>
      <li><%= posts[idx]['text'] %></li>
      <% } %>
    </ul>

</body>
</html>
